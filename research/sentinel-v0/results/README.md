# Sentinel v0 Results

Commit only compact derived results that are lawful to redistribute and resolve to verified manifests. Generated local outputs belong under `results/generated/`, which is ignored.

Do not commit raw provider bars, provider responses, credentials, model weights, or caches.
